
const API = require('../index')
const ENDPOINTS = require('../config/endpoints')

test.todo('API is created correctly')

test.todo('API createRequest should add extra info to config')

test.todo('login with api credentials')

test.todo('login with other credentials')

test.todo('outbreaks should call correct endpoint with autologin')

